export interface Position {
  longitude: number;  // Horizontal rotation in degrees (0-360)
  latitude: number;   // Vertical rotation in degrees (-90 to 90)
  zoom: number;       // Zoom level (0-100)
} 